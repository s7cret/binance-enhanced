# .env.example
# Example environment file for Binance skill.
# Copy to .env and fill values. Use TESTNET section when testing.

# ---------- Production (MAINNET) ----------
# Export these variables for normal trading on Binance production API.
BINANCE_API_KEY="your-production-api-key"
BINANCE_SECRET="your-production-api-secret"
# Optional: override base URL if using a proxy or internal gateway
BINANCE_API_BASE_URL="https://api.binance.com"
BINANCE_FUTURES_API_BASE_URL="https://fapi.binance.com"

# Set to "live" for executing real trades. Use "paper" or "dry-run" for safety wrappers.
TRADE_MODE="live"  # values: live | paper | dry-run

# Default logging level: DEBUG, INFO, WARN, ERROR
LOG_LEVEL="INFO"

# Referral ID (optional) - реферальный код от Binance
# Полная ссылка: https://accounts.binance.com/register?ref=Q4H0BOOA
REFERRAL_ID="Q4H0BOOA"

# Security / operational
# Optional: restrict IPs on Binance dashboard and set here for reference (not used by code)
ALLOWED_IPS=""

# ---------- Testnet (SANDBOX) ----------
# Use these for experimenting without real funds. Set TESTNET=true to switch endpoints.
TESTNET=true
BINANCE_TESTNET_API_KEY="your-testnet-api-key"
BINANCE_TESTNET_SECRET="your-testnet-secret"
BINANCE_TESTNET_API_BASE_URL="https://testnet.binance.vision"
BINANCE_TESTNET_FUTURES_API_BASE_URL="https://testnet.binancefuture.com" # if applicable

# Optional: shorter recvWindow for low-latency environments (ms)
RECV_WINDOW=5000

# Optional alerting webhook for critical errors (format: https://hooks.example.com/abcd)
ALERT_WEBHOOK_URL=""

# Notes:
# - Never commit your real API keys into version control.
# - When switching between TESTNET and PRODUCTION, ensure TRADE_MODE and API_BASE_URLs match.
# - For CI, provide secrets via the CI secret store instead of committing a .env file.
